
# ✍️ Bold, Italic, Underline & Strong Tags

## 🔹 Bold `<b>`

Makes text **bold** (visual only).

```html
<b>Bold Text</b>
```

---

## 🔹 Italic `<i>`

Makes text *italic* (visual only).

```html
<i>Italic Text</i>
```

---

## 🔹 Underline `<u>`

Underlines the text.

```html
<u>Underlined Text</u>
```

---

## 🔹 Strong `<strong>`

Makes text **bold + important** (semantic).

```html
<strong>Important Text</strong>
```

---

## 🔹 Difference (Important)

* `<b>` → Bold look only
* `<strong>` → Important meaning

---

## 🔹 Example

```html
<p>This is <b>bold</b> text.</p>
<p>This is <strong>important</strong> text.</p>
<p>This is <i>italic</i> text.</p>
<p>This is <u>underlined</u> text.</p>
```

---

## 🔹 Key Points

- ✔ Use `<strong>` for importance
- ✔ `<b>`, `<i>`, `<u>` are styling
- ✔ Prefer CSS for styling later