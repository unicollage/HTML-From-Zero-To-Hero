

# 🔘 Radio Buttons & Checkboxes



## 🔹 Radio Buttons

* Allow user to **select only one option** from a group
* Use `type="radio"`

**Example:**

```html
<p>Gender:</p>
<input type="radio" name="gender" value="male"> Male
<input type="radio" name="gender" value="female"> Female
```

> All radio buttons with the **same `name`** belong to one group



## 🔹 Checkboxes

* Allow user to **select multiple options**
* Use `type="checkbox"`

**Example:**

```html
<p>Hobbies:</p>
<input type="checkbox" name="hobby" value="reading"> Reading
<input type="checkbox" name="hobby" value="traveling"> Traveling
<input type="checkbox" name="hobby" value="gaming"> Gaming
```



## 🔹 Key Points

* Radio → Single choice per group
* Checkbox → Multiple choices allowed
* Always use `value` to identify what user selects

